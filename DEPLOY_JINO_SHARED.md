# Деплой UpMe на обычный хостинг Jino с поддержкой Node.js

## Отличная новость! 🎉

На обычном хостинге Jino можно запускать Node.js приложения, если подключить услугу **"Поддержка веб-приложений"** за **72₽/мес**.

Эта услуга позволяет запускать:
- Node.js приложения
- Python, Ruby, Java, Go
- CGI скрипты
- SSI инструкции

---

## Подготовка

### Шаг 1: Подключение услуги

1. Зайдите в панель управления Jino
2. Найдите услугу **"Поддержка веб-приложений"**
3. Нажмите **"Подключить"**
4. Стоимость: **72₽/мес**

### Шаг 2: Проверка доступа

После подключения услуги у вас появится:
- Доступ к Node.js
- Возможность запускать приложения через специальный интерфейс

---

## Деплой проекта

### Вариант 1: Полный деплой на Jino (Frontend + Backend)

#### Шаг 1: Подготовка файлов

На вашем локальном компьютере:

```bash
# Сборка backend
cd backend
npm install
npm run build

# Сборка frontend
cd ../frontend
npm install
npm run build
```

#### Шаг 2: Создание .env для backend

Создайте файл `backend/.env`:

```env
PORT=3002
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа
HH_CLIENT_ID=ваш_client_id
HH_CLIENT_SECRET=ваш_client_secret
HH_REDIRECT_URI=https://ваш-домен.ru/api/auth/callback
FRONTEND_URL=https://ваш-домен.ru
HF_API_KEY=ваш_huggingface_api_key
NODE_ENV=production
DATABASE_PATH=./data/upme.db
```

#### Шаг 3: Создание .env.production для frontend

Создайте файл `frontend/.env.production`:

```env
VITE_API_URL=https://ваш-домен.ru
```

#### Шаг 4: Загрузка файлов на хостинг

Через **FTP** или **Файловый менеджер** в панели Jino:

**Структура на хостинге:**
```
public_html/
├── index.html (из frontend/dist/)
├── assets/ (из frontend/dist/assets/)
├── backend/
│   ├── dist/ (из backend/dist/)
│   ├── data/ (создайте папку)
│   ├── logs/ (создайте папку)
│   ├── .env
│   ├── package.json
│   └── node_modules/ (установите на сервере)
└── .htaccess (для frontend)
```

**Что загрузить:**

1. **Frontend:**
   - Все файлы из `frontend/dist/` → в корень `public_html/`

2. **Backend:**
   - Папку `backend/dist/` → `public_html/backend/dist/`
   - Файл `backend/package.json` → `public_html/backend/package.json`
   - Файл `backend/.env` → `public_html/backend/.env`
   - Создайте папки: `public_html/backend/data/` и `public_html/backend/logs/`

#### Шаг 5: Установка зависимостей backend на сервере

Через **SSH** (если доступен) или через панель управления:

```bash
cd public_html/backend
npm install --production
```

Если SSH недоступен, загрузите `node_modules` с локального компьютера (но это займет много времени).

#### Шаг 6: Запуск миграций

```bash
cd public_html/backend
npm run migrate
```

#### Шаг 7: Настройка запуска Node.js приложения

В панели управления Jino найдите раздел для запуска веб-приложений (обычно называется "Веб-приложения" или "Node.js приложения").

**Настройки:**
- **Путь к приложению**: `backend/dist/index.js`
- **Рабочая директория**: `backend/`
- **Порт**: `3002` (или тот, который указан в .env)
- **Переменные окружения**: загрузите из `.env` файла

#### Шаг 8: Создание .htaccess для frontend

В корне `public_html/` создайте файл `.htaccess`:

```apache
# Перенаправление всех запросов на index.html (для React Router)
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>

# Проксирование API запросов на backend
RewriteCond %{REQUEST_URI} ^/api
RewriteRule ^api/(.*)$ http://localhost:3002/api/$1 [P,L]

# Кеширование статических файлов
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
</IfModule>
```

**Важно:** Если Jino не поддерживает проксирование через .htaccess, вам нужно будет настроить это через панель управления или использовать другой подход.

---

## Вариант 2: Гибридный (рекомендуется)

Если настройка проксирования сложная, используйте гибридный подход:

### Frontend на Jino (обычный хостинг)
- Загрузите файлы из `frontend/dist/`
- Настройте `.htaccess` для React Router

### Backend на Railway/Render (облачный сервис)
- Деплой backend на Railway (см. `DEPLOY_SHARED_HOSTING.md`)
- Обновите `VITE_API_URL` в frontend

**Преимущества:**
- ✅ Проще настройка
- ✅ Backend на надежной платформе
- ✅ Автоматические обновления

---

## Настройка домена и SSL

### Шаг 1: Настройка DNS

В панели Jino:
1. Перейдите в раздел "Домены"
2. Добавьте ваш домен
3. Настройте DNS записи (обычно настраиваются автоматически)

### Шаг 2: SSL сертификат

Jino обычно предоставляет бесплатный SSL сертификат:
1. В панели управления найдите раздел "SSL"
2. Активируйте бесплатный сертификат Let's Encrypt
3. Примените к вашему домену

---

## Проверка работоспособности

### 1. Проверка frontend

Откройте в браузере: `https://ваш-домен.ru`

### 2. Проверка backend

```bash
# Через curl или в браузере
curl https://ваш-домен.ru/api/health
```

Или проверьте логи в панели управления Jino.

### 3. Проверка API запросов

Откройте DevTools (F12) и проверьте, что запросы к `/api/*` проходят успешно.

---

## Обновление кода

### Через FTP/Файловый менеджер:

1. Соберите проект локально:
   ```bash
   npm run build
   ```

2. Загрузите новые файлы:
   - `frontend/dist/*` → `public_html/`
   - `backend/dist/*` → `public_html/backend/dist/`

3. Перезапустите приложение в панели Jino

### Через Git (если доступен):

```bash
cd public_html
git pull
cd backend
npm install --production
npm run migrate
# Перезапустите приложение в панели
```

---

## Важные моменты

### 1. Ограничения обычного хостинга

- ⚠️ Может быть ограничение на использование ресурсов (CPU, RAM)
- ⚠️ Может быть ограничение на время выполнения скриптов
- ⚠️ Может не быть доступа к SSH
- ⚠️ Может быть ограничение на размер файлов

### 2. База данных SQLite

SQLite подходит для небольших проектов. Убедитесь, что:
- Папка `backend/data/` доступна для записи
- Права доступа установлены правильно (обычно 755)

### 3. Логи

Логи будут сохраняться в `backend/logs/`. Убедитесь, что папка доступна для записи.

### 4. Переменные окружения

В панели управления Jino может быть раздел для настройки переменных окружения. Используйте его вместо `.env` файла, если это возможно.

---

## Troubleshooting

### Ошибка: "Cannot find module"

```bash
# Убедитесь, что node_modules установлены
cd backend
npm install --production
```

### Ошибка: "Port already in use"

В панели Jino проверьте, какой порт используется для вашего приложения. Обновите `PORT` в `.env`.

### Ошибка: "Database locked"

SQLite может блокироваться. Убедитесь, что:
- Только одно приложение использует БД
- Права доступа к файлу БД правильные

### API запросы не проходят

1. Проверьте настройки проксирования в `.htaccess`
2. Проверьте, что backend запущен
3. Проверьте CORS настройки в backend

---

## Стоимость

- **Обычный хостинг Jino**: ~200-300₽/мес (если уже есть)
- **Поддержка веб-приложений**: +72₽/мес
- **Итого**: ~272-372₽/мес

**Сравнение с VPS:**
- VPS: от 200₽/мес, но нужна настройка
- Обычный хостинг + услуга: ~272₽/мес, проще настройка

---

## Рекомендации

### Для старта:
✅ Используйте обычный хостинг Jino + услугу поддержки веб-приложений

### Для продакшена с большой нагрузкой:
✅ Рассмотрите VPS или гибридный подход (frontend на хостинге, backend на облаке)

---

## Полезные ссылки

- [Документация Jino](https://jino.ru/help/)
- [Поддержка Jino](https://jino.ru/support/)

---

Готово! Ваш проект должен работать на обычном хостинге Jino с подключенной услугой поддержки веб-приложений.

