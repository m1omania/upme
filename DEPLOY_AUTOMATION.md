# Инструкция по настройке автоматического деплоя

## Настройка GitHub Secrets

1. Зайдите в ваш репозиторий на GitHub: https://github.com/m1omania/upme
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:

### SERVER_HOST
```
53893873b619.vps.myjino.ru
```

### SERVER_USER
```
root
```

### SERVER_PORT
```
22
```
(или `49376`, если SSH будет работать на этом порту)

### SERVER_SSH_KEY
Содержимое вашего приватного SSH ключа. Если ключа нет, создайте его:

**На вашем локальном компьютере:**
```bash
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions_deploy
```

Затем скопируйте содержимое приватного ключа:
```bash
cat ~/.ssh/github_actions_deploy
```

И добавьте его в GitHub Secrets как `SERVER_SSH_KEY`.

**На сервере добавьте публичный ключ:**
```bash
# На сервере выполните:
mkdir -p ~/.ssh
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

Публичный ключ можно получить:
```bash
cat ~/.ssh/github_actions_deploy.pub
```

## Вариант 1: Простой деплой через git pull (Рекомендуется)

Этот вариант не требует SSH и работает через веб-консоль.

### 1. Создайте скрипт деплоя на сервере

Выполните на сервере (через веб-консоль):

```bash
# На сервере:
cd /var/www/upme
cat > deploy.sh << 'EOF'
#!/bin/bash
set -e

echo "🔄 Обновление проекта..."

cd /var/www/upme

echo "📦 Обновление зависимостей..."
cd backend && npm install --production && cd ..
cd frontend && npm install && cd ..

echo "🔨 Сборка backend (если нужно)..."
cd backend
if [ -f "tsconfig.json" ]; then
  npm run build || echo "⚠️ Backend build skipped (already built)"
fi
cd ..

echo "🔨 Сборка frontend (если нужно)..."
cd frontend
if [ -f "vite.config.ts" ]; then
  npx vite build || echo "⚠️ Frontend build skipped (already built)"
fi
cd ..

echo "🔄 Перезапуск backend..."
pm2 restart upme-backend || pm2 start ecosystem.config.js

echo "✅ Деплой завершен!"
pm2 status
EOF

chmod +x deploy.sh
```

### 2. Настройте автоматический деплой через cron (опционально)

Для автоматической проверки обновлений каждые 5 минут:

```bash
# На сервере:
crontab -e

# Добавьте строку:
*/5 * * * * cd /var/www/upme && git fetch && [ $(git rev-parse HEAD) != $(git rev-parse origin/main) ] && git pull && ./deploy.sh >> /var/log/upme-deploy.log 2>&1
```

Или используйте более простой вариант - проверка при каждом запросе (через webhook).

### 3. Ручной деплой

После каждого `git push` в main, зайдите на сервер и выполните:

```bash
cd /var/www/upme
git pull origin main
./deploy.sh
```

---

## Вариант 2: Полностью автоматический через SSH

### 1. Создайте скрипт деплоя на сервере

Проверьте подключение с вашего компьютера:
```bash
ssh -p 22 root@53893873b619.vps.myjino.ru
```

Если SSH не работает, используйте альтернативный вариант (см. ниже).

## Альтернативный вариант (без SSH)

Если SSH не работает, можно использовать GitHub Actions только для сборки, а деплой делать вручную или через другой механизм.

### Вариант 1: GitHub Actions собирает, вы загружаете

1. GitHub Actions собирает проект
2. Создает артефакт
3. Вы скачиваете артефакт и загружаете на сервер через веб-консоль

### Вариант 2: Webhook деплой

Настройте webhook на сервере, который будет получать уведомления от GitHub и запускать деплой.

## Использование

После настройки:

1. **Автоматический деплой**: При каждом `git push` в ветку `main` будет автоматически запускаться деплой
2. **Ручной запуск**: В GitHub → Actions → Deploy to Production → Run workflow

## Проверка

После деплоя проверьте:
- Сайт работает: https://upme.pro
- Backend запущен: `pm2 status`
- Логи без ошибок: `pm2 logs upme-backend`

